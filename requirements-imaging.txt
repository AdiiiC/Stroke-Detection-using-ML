# Deep-learning imaging branch (CT/MRI). Install on top of requirements.txt.
# Use a CUDA build of torch on GPU machines; the CPU wheel works for the
# --quick smoke test.
torch>=2.2
torchvision>=0.17
grad-cam>=1.5
albumentations>=1.4
opencv-python-headless>=4.9
pydicom>=2.4
nibabel>=5.2
scikit-image>=0.22
kaggle>=1.6
tqdm>=4.66
